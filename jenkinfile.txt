pipeline{
    agent any
    stages{
        stage("git"){
            steps{
                git 'https://github.com/abusufian12/simple-calculator.git'
                sh 'npm install'
            }
        }
        
        //stage('Run tests') {
          //steps {
            // Run Jest tests
            // Generate test reports (e.g., JUnit XML or HTML reports)
            // Optionally, calculate code coverage and generate coverage reports
          //}
        //}
    }
}
